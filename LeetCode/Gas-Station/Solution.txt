1class Solution {
2public:
3    int canCompleteCircuit(vector<int>& gas, vector<int>& cost) {
4        int n = gas.size ();
5        deque<int>d;
6        for(int i=0;i<n;i++)d.push_back(gas[i]-cost[i]) ;
7        int sum =0 ,l =0;
8        vector<int>mark(n+1,0);
9        for(int i=0;i<n;i++)
10        {
11            sum+=gas[i]-cost[i];
12            if(sum<0){
13                mark[i]=1;
14                sum=0;
15            }
16        }
17        for(int i=n-2;i>=0;i--)
18        {
19            mark[i]|=mark[i+1];
20        }
21        for(int i=0;i<n;i++)
22        {
23            if(mark[i]){
24                d.push_back(d.front());
25                d.pop_front();
26            }
27            else break;
28        }
29        sum=0;
30        for(int i=0;i<n;i++)
31        {
32            sum+=d.front();
33            if(sum<0)return -1;
34            d.pop_front();
35        }
36        for(int i=n-1;i>=0;i--)
37        {
38
39            if(mark[i])return (i+1)%n;
40        }
41        return 0;
42    }
43};